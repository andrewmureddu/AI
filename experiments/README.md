# Experiments

Runnable, reproducible tests of specific questions — the restraint stroke of the
[research rhythm](../METHODOLOGY.md). Each experiment is self-contained (prefer pure
numpy; state any install), deterministic where possible, and ends with an honest
verdict including what it does *not* show.

| Experiment | Tests | Result |
|------------|-------|--------|
| [`S3-fisher-geometry/`](./S3-fisher-geometry/) | [S3](../questions/SPECULATIVE.md): does a Fisher/statistical-curvature quantity spike at a phase transition, in physics *and* in learning? | **Partially confirmed** — χ→∞ at the Ising T_c; inverse-Fisher blows up exactly at the double-descent test-error peak (P/N=1). Grokking still open. |
| [`S18-invariance-transfer/`](./S18-invariance-transfer/) | [S18](../questions/SPECULATIVE.md) / the [prediction-field](../PREDICTION-FIELD.md) falsifier: does invariance-across-environments predict transfer? | **Falsifier survived** — train-measured coefficient instability ranks held-out transfer error at ρ=0.985; the most in-distribution-predictive feature is the worst-transferring. Confirmed in a constructed SCM; real cross-domain test still open. |
| [`ladder-vs-transfer/`](./ladder-vs-transfer/) | [SYNTHESIS §6.1](../SYNTHESIS.md): does ladder *level* predict transfer across genuinely different mechanisms? | **Confirmed, then corrected** ([paper 1](../paper/transfer-cliff.md)) — level orders transfer (L1 0.03, L2 0.48, L3 0.92, L4 0.89) and L3≈L4, so mechanism and theorem transfer equally. The *"cliff at L2/L3"* step reading is **retracted**: adjacent steps are L1→L2 = +0.451 ≈ L2→L3 = +0.432, so the L2/L3 step is not uniquely large. What is real is a **ceiling** — the ceiling is now measured (0.920±0.023 by scoring a law against an independent draw of itself), L3 reaches 0.916 (complete transfer), and L2 saturates at ~0.54 no matter how much evidence it gets. Controlled/synthetic; real-data version still open. |
| [`S23-attention-bayes/`](./S23-attention-bayes/) | [S23](../questions/SPECULATIVE.md): is softmax attention one step of the [universal update](../invariants/mirror-descent-update.md)? | **Confirmed exactly** — attention weights = Bayes posterior over memories to 1.4e-17; attention temperature = the update's η = 1/σ²; plain dot-product attention is a biased posterior, exact once ‖k‖² is corrected (explains QK-norm). Attention is now a confirmed L3 instance of the universal update. |
| [`real-transfer/`](./real-transfer/) | [SYNTHESIS §6.1](../SYNTHESIS.md): does the [prediction-field frame](../PREDICTION-FIELD.md) hold on data I did **not** construct? | **Passed both legs** — Leg A: on the real diabetes dataset, invariance predicts transfer at ρ=0.983 (bmi/s5 stable+transfer, sex neither). Leg B: Benford transfers across 2ⁿ/3ⁿ/n!/Fibonacci (0.97) and fails on controls (0.00). Frame now empirically load-bearing. Honest confound: Leg A can't separate invariance from signal strength (S18 controls that; this doesn't). |
| [`S25-channel-independence/`](./S25-channel-independence/) | [S25](../questions/SPECULATIVE.md) / the 🔴 tier of the [prediction field](../PREDICTION-FIELD.md) — the first test of the *metaphysical* reading rather than the operational core: are the field's singularities independent of the measurement channel? | **Clean claim fails, with structure.** Singularities are *invariant* under generic channels and *covariant* under structured ones: F_c(T) = F_eff(T′)·(dT′/dT)² exact to **1e-14** — an RG composition law, not independence. Per the pre-registered criteria the 🔴 tier stays scaffolding-only and was **not** promoted. One pre-registration error on record (parity is blind only at odd N). |
| [`S25-rlct-singularity/`](./S25-rlct-singularity/) | [S25](../questions/SPECULATIVE.md): where [S3](./S3-fisher-geometry/)'s Fisher metric degenerates, does Watanabe's learning coefficient λ (RLCT) — not parameter count d/2 — price generalization and transfer? | **Confirmed, all registered predictions** — free-energy slopes recover exact RLCTs on solved singular models (0.508 vs ½ where d/2=1; 0.248 vs ¼); at the random-feature population singularity λ(P)=min(P,D)/2 with a kink at P=D (plateau at D/2 to 0.08 while d/2→30); Bayes excess risk *and* covariate-shift transfer error plateau with λ (ratios 0.97 vs d-scaling's 3). Bonus: a finite-size critical window blurs λ̂ right at the singular point, matching S3's phenomenology. Limits: rank-type (simplest) singularities; shift ≠ cross-domain transfer. |
| [`S26-llc-trajectory/`](./S26-llc-trajectory/) | [S26](../questions/SPECULATIVE.md) (= S11 × S25): does learning *flow toward* singular (low-λ) regions — is degeneracy an attractor of SGD? | **Mixed, honestly split** — SGLD local-λ̂ estimator validated to ≤11% against S25's quadrature; the endpoint is deeply singular (λ̂=1.38 vs d/2=8, registered P1 ✓) **but** the attractor claim is *falsified*: post-convergence λ̂ drifts up with residual fitting (P3 ✗) and noiseless full-batch GD lands at the same λ̂=1.40 (post-hoc control). Deflationary survivor: the low-loss set is generically singular — degeneracy is where you end up, not what pulls you. Also mapped: λ̂ is meaningless at non-critical points (P2 not evaluable); plateau-rich (grokking) tasks are the escalation. |
| [`S28-sgd-charges/`](./S28-sgd-charges/) | [S26(iii)](../questions/SPECULATIVE.md): is SGD's stationary law coordinatized by its conserved charges? | **Confirmed as a prethermalization plateau** — Q conserved in the flow limit (drift ∝ lr); weight-decay breaking follows dQ/dt=−4λQ to 1e-5; stationary norm = √(Q²+4w*²) predicted from the charge alone to 3e-4; charge quasi-conserved (erosion 5.6e-7/step, ~1.8M-step window) — the GGE logic transfers *with* its known failure mode. |
| [`M3-molloy-reed-rewiring/`](./M3-molloy-reed-rewiring/) | [M3](../questions/L3-MECHANISMS.md) = [essay 04's falsifier 1](../essays/04-the-periphery-split.md), never previously run: does the degree histogram — everything a Φ on node degrees can see — determine the percolation threshold? | **No, and the essay's premise survives.** At **bit-for-bit identical** degree sequences, rewiring moves p_c by **35% / 72% / 148%** (bimodal/trimodal/power-law) while random swaps move it <2.5%. The pair statistic recovers it: predicted shift ratios match measured to **≤4.6%** (a 41% shift predicted to 1.7%). Degrees+assortativity is *also* insufficient — the feasible λ_max at fixed r spans **11.8%** (exact polytope scan), and rewiring to that maximum gives two graphs with Δr = **1e-5** and thresholds **11.9%** apart. Boundary, signed: e_{jk} holds only while locally tree-like — **+10.4%** error at clustering 0.030, **+124%** at 0.211. Reading: connectivity is a Φ-derivative of the *edge-pair* measure, not the node measure — M11's "Φ of what?" from the connectivity side. |
| [`M11-stochastic-resetting/`](./M11-stochastic-resetting/) | [M11](../questions/L3-MECHANISMS.md): is stochastic resetting *one* mechanism across diffusive search, enzymatic turnover, and randomized backtracking — or three fields separately noticing restarts help? | **Identity confirmed** — at the optimal rate the restarted process has **CV = 0.998 / 1.001 / 1.028** in the three domains (un-reset CVs 167 / 1.71 / 2.71, one with *infinite* mean): the same universal constant from unrelated processes. Criterion CV>1 misclassifies 0/10; crossing located at σ=0.856 vs √(ln2)=0.833; diffusion optimum 2.62 vs analytic 2.5396; whole restart curve predicted from the un-reset distribution alone (≤2.9%), *including* where ⟨T⟩=∞. Two sign flips **within** single domains (one enzyme scheme: hurts→helps 25×; one search paradigm: 3-SAT no help → QWH 19.4×). **Framing retired:** not a second axis — the exponential is MaxEnt on [0,∞) at fixed mean with CV=1, so this is Φ's log-partition *form* on a different base variable (exit time, not state). Three estimator failures diagnosed en route. |
| [`M15-adiabatic-charges/`](./M15-adiabatic-charges/) | [M15](../questions/L3-MECHANISMS.md): is SGD's quasi-conserved charge an *adiabatic invariant* — i.e. is the averaging mechanism running, or is "prethermalization plateau" just a label? | **Confirmed, ordinary-averaging regime** — the symmetry generator is orthogonal to the *stochastic* gradient for every realization, so first order vanishes identically and drift is exactly second order (1.6e-16 scalar, 1.0e-15 wide net). Registered scaling k ∝ η^2.021 σ^1.999 B^−1.003; parameter-free rate η²σ²E[x²]/B good to 2%. Discriminator: power law beats exp(−c/η) by **ΔAIC = 73.6** — no exponential protection. Breakdown at **R = 1.13** (suff_err = 1.12·R^−0.97). Corrects S26's erosion to 7.30e-7/step (window 1.37M). One pre-registered estimator failed and was diagnosed (saturation, slope 0.39 not 1); corrected observable gives τ ∝ η^−2.038. ML leg only — cross-domain identity untested. |
| [`S7-critical-slowing/`](./S7-critical-slowing/) | [S7](../questions/SPECULATIVE.md) + the middle-zone reduction: is slowing-down one signal, and is it Φ's Hessian softening? | **Mechanism confirmed, exponent refuted** — τ·λ_min = 0.94±0.13 across saddle-node, Ising, and GD-at-the-MP-edge (one relation, no tuning); divergence exponents are domain-specific (−½/−1/−2). Fourth arrival at the ∇²Φ singular-set object. Near-critical nonlinear bias documented (leg B). |
| [`PA-spectral-gap/`](./PA-spectral-gap/) | [P-A](../invariants/FLOORS.md) — the [floor sort](../invariants/FLOORS.md)'s prediction about [#19](../invariants/spectral-gap.md), an entry assigned to floor 3 with no evidence: is gap-closure the ∇²Φ degeneracy? | **Floor assignment confirmed, in the *directional* form; the "one number" reading demoted.** With Φ fixed in advance (the SCGF of a time average), the slow mode carries the whole divergence (Λ''·gap = 2⟨f,v₂⟩² = 2.0000) — **but for an observable orthogonal to it, Λ'' is unchanged (1.00×) while the gap falls 31×**, so this is a null-direction fact, not a scalar one. Three of #19's four readouts are one number *by construction* (stated in advance, so not evidence); the fourth, the Kuramoto threshold, gives K_c·λ₂ → a family-specific constant spanning 0.410–0.632 — the gap absorbs bridge multiplicity exactly but not size asymmetry. **Costs an existing result:** in a double well the gap falls 3540× while local curvature *rises* 6×, so S7's τ·λ=1 is a **single-basin law**. One registered threshold (>2× across families) failed as written — the spread is 1.54×. |
| [`PD-allometry-reduction/`](./PD-allometry-reduction/) | [P-D](../invariants/FLOORS.md) — the [floor sort](../invariants/FLOORS.md)'s only *negative*, about [#9](../invariants/scaling-allometry.md), a seed entry: is Kleiber's 3/4 a Φ-derivative, or a log-ratio of the branching scheme? | **Confirmed on every registered leg, and the exponent is now a law.** θ = **min(1, ln n/−ln(β²γ))** to **7.1e-11** over 22 (scheme, n) pairs, with n cancelling identically (spread **4.0e-15**). Six decades on six dimensionful quantities move θ by **1.1e-8**; the flow law's *structural* exponent moves it a lot — magnitudes invisible, counts not. The strawman was avoided by registering the factoring claim, since WBE's derivation *does* optimize: six cost functionals select six β, and θ is recovered from (β,γ) alone with the functional absent (≤**1.5e-14** in five of six). **The variational route gives the wrong number** — dissipation minimization yields θ = **1** for Poiseuille and every steeper flow law, reaching 3/4 only at R ∝ l/r², which no viscous flow obeys. Φ attempt fails as registered: ∇²log Z **rank 1 for every λ**, conjugate ratio constant to 1.1e-13, against a control swinging **151%**. Independent second route: θ = d/(d+1) = 0.663/0.749/0.805 vs 0.667/0.750/0.800. Two registered items failed and are on record (the MST construction; the mean-field control, itself degenerate for the reason under study). Bonus: θ is **non-analytic** at nβ²γ = 1 and **Murray's law sits exactly on the kink**. |
| [`D7-composition-lens/`](./D7-composition-lens/) | [D7](../questions/UNKNOWN-LAWS.md): is there a floor 0 under the tower, and is it a composition law? Tests [`derivations/D7-composition-lens.md`](../derivations/D7-composition-lens.md). | **Floor 1 takes an input, and it is additivity.** *Leg C* — six statistics of one charge, tilted to equal informativeness: the ensemble family closes for **exactly** the additive ones (≤**3.2e-14**) and for none of the others (≥**8.8e-4**), with linearity refuted by 3Q−7 (closes anyway), monotonicity by three rows that have it and fail, and convexity by Q² and √Q failing identically at opposite curvature. No dynamics and no group appear anywhere in the leg, which is what makes it a claim about a layer *under* floor 1. *Leg D* — Curie–Weiss, exact: δ₂ = **0.00240 / 0.29364 / 0.50010** against the registered 0 / 1−2^{−½} = 0.29289 / 0.5, with Var ~ N^1.022 / N^**1.504** / N^2.002. The middle value is anomalous — inherited from a critical exponent — so **criticality is an order-2 extensivity failure**, and SSB reaches the same order by a different mechanism. Heavy tails have no order at all (δ₂ centred −0.39…−2.93, spread 1.8…7.4, vs a Gaussian control at 0.00±0.03) — which retrodicts the sort's **P-B**. *Leg B* — the concavity defect scales as N^**1.0252** for mean-field q=3 with c<0 over **68.6%** of the energy range, so Φ\* is the concave hull rather than s and **floor 2's Legendre face breaks where composition does**. **A pre-registered hypothesis was killed by its own control:** mean-field q=2 is equally long-range and its defect is *exactly zero*, so "long-range ⇒ non-additive" is false — long range opens the possibility, a first-order transition realizes it. Honest: **two registered items failed** (leg C's growth exponent, +1.00 registered vs −0.87/−1.51/−1.82 measured, killed by my own tilt normalization; leg D3's discriminator, mis-stated); legs A and E1 are **declared identities** carrying no weight; the forceful d ≥ 2 short-range control is not run; three code bugs were found and are recorded in the source. |
| [`D6-support-singularities/`](./D6-support-singularities/) | [D6](../questions/UNKNOWN-LAWS.md) — the remainder [D2](../derivations/D2-gauge-of-the-tower.md) left: support-type singularities (S5's type S) have no *p*, so are they a separate class, is p = ∞ the label, or was [D1](./D1-chart-invariance/) a special case? | **D1 was a special case.** Writing Φ's two leading terms as A\|y\|^q1 + B\|y\|^q2 with A → 0, the crossover is A_c ~ D^(1−q1/q2); **q1 = 2 is what smoothness forces** (giving D1's (p−2)/p with q2 = p) and **support loss is q1 = 1**, where D1's formula is undefined rather than wrong. The observable had to change too — D1's Var·λ/D needs a curvature a kink lacks, so shape (excess kurtosis) is used instead, defined at q=1 (+3.0000) and q=2 (0.0000) alike. **All three at-risk legs pass, and none are physics:** L1-penalized logistic regression (all orders) gives τ_c ~ D^**0.5000** vs 0.5000; **M/M/1 has no crossover at all** as registered — kurtosis pinned at 6.0000 over four decades of 1−ρ, spread 0.0111 — so support loss is not p = ∞ but an empty q2 slot; and the hard-wall corner q2 → ∞ gives **0.9979** vs 1. Synthetic grid confirms the form to 1e-14 across nine pairs including q=0.5 and q=8. **The classifier is the ratio q1/q2** — exactly the form D2 derived the chart-free residue must take, now on the class D2's codimension argument could not reach. Honest: P1/P2 are identities (**P2 was mis-registered as at-risk — third such slip, all the same failure of calling a consequence of my own construction a test of it**); the ratio claim is untested on real models; remainder narrows to essential singularities only. |
| [`D2-gauge-group/`](./D2-gauge-group/) | [D2](../questions/UNKNOWN-LAWS.md) / the map's standing **P0**: the four things that [refused the tower](../invariants/FLOORS.md) share a log-ratio signature — fourth floor, floor 1 in disguise, or coordinate freedom? Tests [`derivations/D2-gauge-of-the-tower.md`](../derivations/D2-gauge-of-the-tower.md). | **P0 answered: there is no fourth floor.** The floors are defined up to **G_diff** (reparameterizations smooth *at* the singular point); cross-domain comparison has only **G_pow** (φ ~ ε^a, smooth away from it but not at it); **the scheme layer is the difference.** RG eigenvalues are G_diff-invariant by conjugation (**5.3e-15** over 6 random conjugations) and G_pow-covariant as y → a·y (**≤2.4e-12**) — identical to D1's chart order, so all the log-ratio refusers are one object: transformation data, which is not a fact on any floor. Legs recover y=1 (1D Ising), ν=1.338266 (diamond lattice), δ=4.669201 (Feigenbaum). **Chart-free residue = the codimension p−2**: β/k = **0.5000** (p=4) and **0.2503** (p=6) vs 1/(p−2) on full models, while β spans 4× and k spans 2×. Explains `FLOORS.md`'s unexplained asymmetry (floor 2 collapses because G_pow=G_diff away from the singularity; floor 3 stratifies because the group enlarges exactly there) and settles D1's loose end (**within a domain exponents are facts; across domains only ratios, signs and counts are**). Honest: most legs are theorem checks, both at-risk legs are physics, the common-a commitment is load-bearing (P3 shows independent re-charting kills the ratios), and one mid-run correction is on record (the first "resampling" was a no-op). |
| [`D1-chart-invariance/`](./D1-chart-invariance/) | [D1](../questions/UNKNOWN-LAWS.md), the first **discovery**-register stone: are floor-3 exponents domain-specific (as [S5](./S5-noise-thresholds/) and [S7](./S7-critical-slowing/) concluded), or is a bare exponent simply not a property of the singularity? | **Chart claim confirmed in the strong form; formula deflated to N0–N1 as its own prior-art note warned.** A floor-3 degeneracy carries two integers — chart order *k* (gauge) and degeneracy order *p* (invariant) — with λ_c ~ D^{(p−2)/p}. *k* removable **by inference** (fold k=½ and SIS epidemic k=1, *g* differing 3×: bare exponents 0.667 vs 0.333, λ-chart **0.33333 vs 0.33333**; prefactor ratio 2.0801 vs 3^{2/3}=2.0801 predicted) and **by manipulation** (under ε′=ε^a the bare exponent moves by exactly a, the λ-chart exponent not at all). Both **at-risk** all-orders legs pass: 0.4961 (p=4) and 0.6635 (p=6) vs 0.5000/0.6667. The bare chart **inverts** the classification — p=3 and p=6 agree to three decimals while two p=3 systems differ 2×. Null control 4.4e-16; τ·λ = 1.005/0.987/0.935/0.997 extends S7 to epidemiology and a tricritical point. **Declared in advance:** legs A/B are identities, not evidence. **One registered direction refuted** (P8's sextic window is the large-D end); mechanism survived as a post-hoc D_× ~ δa³, measured δa^3.211. |
| [`S5-noise-thresholds/`](./S5-noise-thresholds/) | [S5](../questions/SPECULATIVE.md): are Shannon capacity, Eigen's error catastrophe and the quantum fault-tolerance threshold one redundancy-vs-noise transition? | **Falsifier fires — and the replacement is sharper.** Exponents span 1.00→3.17, varying *within* domains, not across them. α is a **degeneracy order**: 2 where the conditional laws merge smoothly (forced by the Fisher expansion — BSC, BI-AWGN, majority-repair quasispecies), 1 where the informative support loses mass (BEC, Z-channel), ln n₀/ln(t+1) where the threshold is a decoder's RG fixed point (4 concatenated codes, to 1.6e-4). Discriminator verified: α = order of vanishing of χ²_sym to 0.0015. Eigen's bare threshold is the **zero-redundancy corner** of Shannon's (L·μ_c = ln σ from exact dynamics); add a decoder and it moves to μ=½ with r_min·δ² = 1.322 flat — the coding gap is a constant (≈3.8), not an exponent. Fifth arrival at ∇²Φ's singular set, the first to measure its *order*. |

## Conventions

- Keep it cheap and deterministic; seed everything.
- Save raw outputs (CSV/JSON) next to the script so results are auditable.
- The `README.md` states the claim, the result, and the honest limits — a passing
  number is not a finding until its boundary conditions are written down.
- A surviving result graduates its object toward a full
  [`invariants/`](../invariants/) entry and gets a [research-log](../research-log/)
  note.
