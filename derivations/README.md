# Derivations

Analytical restraint — the pen-and-paper counterpart of
[`experiments/`](../experiments/). A derivation takes a speculative leap, tries to
prove the identity *and* find where it breaks, and ends with a level verdict plus
explicit boundary conditions. No code; the discipline is mathematical rigor and
honest scoping.

| Derivation | Tests | Result |
|------------|-------|--------|
| [`S25-one-parameterness.md`](./S25-one-parameterness.md) | [S25](../questions/SPECULATIVE.md)'s stress test: the most adversarial channels of a one-parameter Boltzmann family only reached rank-1 fraction ≈0.95 — why do channels resist leaving one-parameterness? | **Proved.** A channel stays exactly one-parameter iff the conditional mean energy μ_y(β) is constant across outcomes y at every β — the exact obstruction — and a quantitative bound on the departure follows from the conditional energy variance. Holds for stochastic channels too, nothing uses determinism. Verified numerically in [`experiments/S25-channel-independence/verify_theorem.py`](../experiments/S25-channel-independence/verify_theorem.py). |
| [`S1-universal-update.md`](./S1-universal-update.md) | [S1](../questions/SPECULATIVE.md): are replicator / multiplicative-weights / Bayes / Gibbs one update? | **L3, confirmed & bounded** — all are `x_i ∝ x_i·exp(−η g_i)` (entropic mirror descent); global convergence *not* shared (cycling in games), retired to L1. |
| [`essay3-method-as-update.md`](./essay3-method-as-update.md) | [Essay 3 §4](../essays/03-expansion-and-restraint.md): is the expansion⇄restraint method itself an instance of (★)? | **Retired as stated** — method = replicator–mutator, not mirror descent: level assignment is per-entry Bayes ((★), trivially L3); attention allocation L1–L2 (mechanism unwritten, one epoch); expansion provably outside (★) (multiplicative updates preserve zeros). Self-licensing worry deflates 🔴→🟡. |
| [`S27-control-split.md`](./S27-control-split.md) | [S25](../questions/SPECULATIVE.md): does control split into a Φ-half and an irreducible reachability half? | **Falsifier fired, conjecture upgraded** — controllability Gramian = noise-ensemble covariance (∇²Φ); min control energy = large-deviations rate fn (Legendre dual of Φ), exact linear-Gaussian. But binary reachability reduces only to Φ's *singular set*. New claim: connectivity facts are Φ-boundary facts; map is two-layered (regular Φ / singular Φ) + a possible symmetry axis. |
| [`D2-gauge-of-the-tower.md`](./D2-gauge-of-the-tower.md) | [D2](../questions/UNKNOWN-LAWS.md) / the map's standing **P0**: are the four tower-refusers a fourth floor, floor 1 in disguise, or coordinate freedom? | **No fourth floor — the scheme layer is the tower's structure group.** The floors are defined up to **G_diff** (reparameterizations smooth *at* the singular point); cross-domain comparison has only **G_pow** (φ ~ ε^a); the log-ratio signature is the difference. RG eigenvalues are conjugation-invariant and G_pow-covariant (y → a·y) — identical to D1's chart order, so all the refusers are transformation data, which is not a fact on any floor. **Chart-free residue = the codimension p − 2**, i.e. D1's degeneracy order; β/k = 1/(p−2) verified on full models. Also derives `FLOORS.md`'s unexplained asymmetry (floor 2 collapses / floor 3 stratifies = which group acts where) and reconciles with universality (**within a domain exponents are facts; across domains only ratios, signs, counts**). Load-bearing caveat: the ratios need the **common-a** commitment. Numerics: [`experiments/D2-gauge-group/`](../experiments/D2-gauge-group/). |
| [`D7-the-residue.md`](./D7-the-residue.md) | [D7](../questions/UNKNOWN-LAWS.md) / the successor to the **P0**: D2, D6 and P-D reached the chart-free residue by three routes and produced three objects that cannot state each other — is there a general form? | **Yes, and it is the Π-theorem with the chart as the single dimension.** The residue is the log-slope vector modulo the **diagonal** ℝ⁺ — equivalently the slopes of observables against *other* observables, d ln O_i/d ln O_j. All three members are that invariant on three slope vectors. Three consequences the members do not individually imply: the count is **n − 1** (first measurement at n > 2); the residue is **not intrinsically discrete**, so D2's integer is Taylor orders' arithmetic and not the residue's; and D2's **common-*a*** caveat is *derived* — it says the action is the diagonal, which is what "one singularity, one family" means, with the product group leaving only signs. States its own boundary: a **derivative with respect to the chart is not an observable**, transforms affinely as y ↦ a·y + (a−1), and destroys the ratios if included. Numerics: [`experiments/D7-residue-projective/`](../experiments/D7-residue-projective/). |
| [`D11-residue-reconciliation.md`](./D11-residue-reconciliation.md) | [D11](../questions/UNKNOWN-LAWS.md): D7/D8's projective/Grassmannian residue, D10's composition-pinning criterion, and D3-ladder-invariance's "fixed point, ratio as eigenvalue" were produced independently and never checked against each other — do they reconcile? | **Yes.** They are one structure: D7/D8's residue is the projectivized eigenvalue vector of a description map's linearization at a fixed point (identity, given D7's own hypotheses — D2's own R1–R3 legs were already this, unnamed); D10 determines which variables carry that structure at all; D3's language is the literature's own frame (Jona-Lasinio's RG-for-CLT programme) for what D7/D8 compute eigenvalues of. Proves, rather than merely observes, why D3's counterexample exists: for the dyadic renormalized-sum map, any law of U makes the scale mixture X=U·Z an exact fixed point, giving an infinite-dimensional continuum sharing one eigenvalue while a scale-invariant shape statistic — provably tangent to the fixed-point set — varies freely. **The residue is necessary but never sufficient wherever the fixed-point set has tangent directions**, and no enlargement of the log-slope construction can close that gap. Numerics: [`experiments/D11-residue-reconciliation/`](../experiments/D11-residue-reconciliation/). |
| [`D10-composition-lens.md`](./D10-composition-lens.md) | [D10](../questions/UNKNOWN-LAWS.md): floor 1 takes the symmetry group as *given*, and there is no outside to give it. Is there a layer underneath, and is it a composition law? | **There is, and it is.** ⊎ — a rule for putting two instances side by side — sits under floor 1 and does four things. **C1**: independent parts multiply, so the unique continuous *additive* summary is Φ = c·ln Z (Cauchy) — the hub's form is downstream of composition. **C2**: the natural-parameter slot takes **additive** statistics, not merely conserved ones (Koopman–Pitman–Darmois), which **corrects `symmetry-sector.md` §2**; measured with no dynamics anywhere in the test, which is what establishes priority. **C3**: a chart is pinned when its variable is additive *or dual to one*, so **the residue is the invariant content of the largest group ⊎ fails to pin** — answering [`FLOORS.md`](../invariants/FLOORS.md) §4's open question, with p−2 and allometry's θ as its two branches. **C4** (the at-risk half): Φ is a CGF, so additivity is one condition *per order*, and **the floors are the orders at which it fails** — order 0 → Φ\* = concave hull ≠ s (floor 2's Legendre face); order 2 → criticality (δ₂ = 0.29364 vs 1−2^{−½}) and SSB (0.50010); no order → heavy tails. Retrodicts P-B. Numerics: [`experiments/D10-composition-lens/`](../experiments/D10-composition-lens/). |
| [`base-variable.md`](./base-variable.md) | [M11](../experiments/M11-stochastic-resetting/)'s "Φ of what?" — the map's oldest live debt, flagged by M3, P-D, P-C and P-K: is the tower one object, or a family indexed by base variable? | **One tower, seen through observables.** Write Φ_Y(λ) = log E_P[e^(λ·Y)] for an observable Y of a master measure. The **form** is portable to every Y (convexity, ∇Φ = mean, ∇²Φ = Cov — two lines of Hölder, so the hub's cross-field recurrence is a property of the transform, not of the world); the **geometry** is not (∇²Φ_Y = Cov(Y); measured spread **413×** across three observables of one measure); base variables are **pushforwards ordered by refinement**, so M11's "family of towers" is answered deflationarily. **The rule that picks the variable is sufficiency** — an invariant is a Φ-fact on Y iff Y is sufficient for it — which makes the question decidable and predicts M3's exact failure ladder. Unasked-for payoff: **`FLOORS.md` §2's "floor 3 stratifies" is forced, into exactly three strata** (3a domain boundary / 3b limit / 3c degeneracy), because analytic-inside-its-domain leaves no fourth option; this makes D1's *p*, D6's q1/q2, D6's support-loss result and P-K's kink one statement. Corollary: **a singular density is not a singular Φ**. And **P-D's "no Φ at all" is downgraded** to a Φ on a **rank-one base variable** (rank 1 to 4.36e-16, effective dimension 2 once an independent statistic is added). Numerics: [`experiments/BV-base-variable/`](../experiments/BV-base-variable/). Honest: a stitching job on old machinery (sufficiency, exponential families, mgf analyticity), and the rule has never been used *prospectively*. |
| [`symmetry-sector.md`](./symmetry-sector.md) | Does the invariance/symmetry sector reduce to Φ (monism), or is it the second primitive? | **Split verdict** — symmetry-*breaking* reduces to Φ's singular set (SSB at non-analyticities; Goldstone = null ∇²Φ); Noether proper does not reduce but *constitutes*: conserved charges are the natural parameters of long-time ensembles (Gibbs/GGE). Map architecture: **one tower, three floors** (symmetry → Φ-regular → Φ-singular). Spawns S26; sharpens S9 to L2-with-L3-route. |

## Convention

A derivation is only "done" when it states the boundary — the regime where the
identity fails. An identity that "always holds" hasn't been pushed hard enough. A
surviving result promotes its object in [`invariants/`](../invariants/) and earns a
[research-log](../research-log/) note; a failed one is logged and the correspondence
marked L1.
